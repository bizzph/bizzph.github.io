/*
  Optional external JSON feeds.

  Each URL must:
  1. Return an array of listing objects matching data/listings.json.
  2. Allow browser requests from your GitHub Pages domain (CORS).
  3. Be public. Never put secret API keys in this file.

  Example:
  window.CEBU_PROPERTY_CONFIG = {
    feeds: [
      "https://your-domain.example/listings.json"
    ]
  };
*/
window.CEBU_PROPERTY_CONFIG = {
  feeds: []
};
